# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @shopy-shopy/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t shopy-shopy-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system shopy-shopy-auth-service && \
          adduser --system -G shopy-shopy-auth-service shopy-shopy-auth-service

COPY apps/auth-service/dist shopy-shopy-auth-service/
COPY apps/auth-service/package.json shopy-shopy-auth-service/
RUN chown -R shopy-shopy-auth-service:shopy-shopy-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix shopy-shopy-auth-service --omit=dev -f install

CMD [ "node", "shopy-shopy-auth-service" ]
